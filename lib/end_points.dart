const LOGIN = 'login';

const HOME = 'home';

String token = '';

const Get_Categories = 'categories';

const FAVORITES = 'favorites';

const PROFILE = 'profile';

const REGISTER = 'register';

const UPDATE_PROFILE = 'update-profile';

const SEARCH_PROFILE = 'search';
